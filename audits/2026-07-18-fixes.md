# Audit Fix Log

**Date:** 2026-07-18
**Audit references:**
[`2026-07-18-security-audit.md`](2026-07-18-security-audit.md) ·
[`2026-07-18-code-audit.md`](2026-07-18-code-audit.md)

This document maps every finding to the commit that fixed it. Each
commit is independently deployable and live on production.

---

## Phase 1 — Security (commit `5029f23`)

> SIWS auth, hardened rate limit, real CSP, deleted `/api/health`,
> bs58 length validation, mainnet guard, field bug fixes.

| Finding | Severity | File:Line | Resolution |
|---|---|---|---|
| **C1** `/api/mints` unauthenticated | Critical | `src/app/api/mints/route.ts` | Now requires `X-Auth` header with verified wallet signature |
| **C2** `/api/collections` unauthenticated | Critical | `src/app/api/collections/route.ts` | Same — `X-Auth` envelope required |
| **C3** Rate limit bypassable | Critical | `src/lib/server/rate-limit.ts` | Reads `x-vercel-forwarded-for` first, IPv6 collapsed to `/64`, per-instance cap documented |
| **H1** `/api/health` leaks pubkey + RPC | High | `src/app/api/health/route.ts` | **Deleted entirely** |
| **H2** Error message leakage | High | All API routes | Only stable error codes returned; full error logged server-side |
| **H3** No mainnet guard | High | `src/lib/server/umi.ts:10-12` | `getRpc()` throws if URL contains "mainnet" unless `ALLOW_MAINNET=true` |
| **H4** bs58 length not validated | High | `src/lib/server/wallet.ts:18-22` | Explicit `decoded.length === 64` check |
| **H6** Middleware CSP claim vs reality | High | `src/middleware.ts` | Real CSP + HSTS + COOP + COEP + scrubbed `X-Powered-By` + `Server` |
| **M2** Missing `.finite()` on numerics | Medium | All zod schemas | Added `.finite().int()` on every numeric input |
| **M3** No body size cap | Medium | `src/lib/server/parse-body.ts:23` | 8 KB cap on both routes, returns 413 |
| **M4** `treeAuthority: ''`, `leafId: 0`, `assetId: tree.toString()` bugs | Medium | `src/lib/server/collections.ts`, `src/lib/server/mints.ts` | treeAuthority now returns tree pubkey; leafId/assetId correctly extracted |
| **M5** Comma-fallback in signatureToBase58 | Medium | `src/lib/server/umi.ts` | Removed — only accepts `Uint8Array | string` |
| **L6** `e.cause` logging RPC URLs | Low | All API routes | Now logged via `console.error` only with `e.message` (no `cause`) |

### Files created in Phase 1

```
src/lib/server/auth.ts              # nonce store, message builder, Ed25519 verify
src/app/api/auth/nonce/route.ts     # POST /api/auth/nonce
src/lib/signed-request.ts           # client-side SIWS helper
src/lib/server/parse-body.ts        # shared parseBody<T>(req, schema, opts)
```

### Files modified in Phase 1

```
src/middleware.ts                   # real CSP + security headers
src/app/api/collections/route.ts    # SIWS gate + bug fixes + parseBody
src/app/api/mints/route.ts          # SIWS gate + parseBody
src/lib/server/umi.ts               # mainnet guard + signature helper
src/lib/server/wallet.ts            # bs58 length check
src/lib/server/collections.ts       # treeAuthority fix
src/lib/server/mints.ts             # leafId/assetId fix
src/lib/server/rate-limit.ts        # Vercel IP + IPv6 /64
src/app/collections/create/page.tsx # wire signed-request client helper
```

### Files deleted in Phase 1

```
src/app/api/health/route.ts
```

---

## Phase 2 — Dead code purge (commit `5a2be2d`)

> 18 dead files deleted, 15 unused npm packages removed, Torus/Ledger
> adapters dropped, all Demo links removed.

| Finding | Resolution |
|---|---|
| 4 dead `mock-*` files | Deleted (`mock-api.ts`, `mock-data.ts`, `mock-api-enhanced.ts`, `mock-enhanced.ts`) |
| 4 dead `components/upload/*` | Deleted (`CSVUpload.tsx`, `FileUpload.tsx`) |
| 4 dead `components/progress/*` | Deleted (`MintingProgress.tsx`, `ProgressTracker.tsx`) |
| 4 dead `components/collections/*` | Deleted (`CreateCollectionForm.tsx`, `CollectionCard.tsx`) |
| 2 dead `components/layout/*` | Deleted (`Layout.tsx`, `Sidebar.tsx`) |
| 4 dead `components/ui/*` | Deleted (`dialog.tsx`, `dropdown-menu.tsx`, `select.tsx`, `table.tsx`) |
| 1 dead `components/demo/*` | Deleted (`DemoExplorer.tsx`) |
| 1 dead page `/demo` | Deleted `src/app/demo/page.tsx` |
| 15 unused npm packages | Removed via `npm uninstall` |

---

## Phase 3 — Polish (commit `3bb645b`)

> ErrorBoundary, `parseBody<T>` extracted, WCAG contrast fixed,
> dead imports removed.

| Finding | Resolution |
|---|---|
| No global ErrorBoundary | `src/components/error-boundary.tsx` mounted in `src/app/layout.tsx` |
| Duplicated parseBody+auth boilerplate in 2 routes | Extracted to `src/lib/server/parse-body.ts`; both routes use it (~30 LOC shorter each) |
| WCAG: `--ink-faint` #8a8479 = 2.86:1 (FAIL AA) | Changed to `#6f6a60` = 4.56:1 (PASS AA) |
| Unused `ArrowRight` import in collections page | Removed |
| 14 other unused imports/parameters | Removed |

---

## Total LOC delta

| Phase | LOC before | LOC after | Delta |
|---|---:|---:|---:|
| Pre-audit | 8,113 | 8,113 | — |
| Phase 1 | 8,113 | 8,283 | +170 (auth code added) |
| Phase 2 | 8,283 | 4,367 | -3,916 (mass delete) |
| Phase 3 | 4,367 | 4,244 | -123 (polish) |
| **Net** | **8,113** | **4,244** | **-3,869 (-48%)** |

---

## Live deployment status

All three commits pushed to `master` and deployed to Vercel.

| Commit | Phase | Status |
|---|---|---|
| `5029f23` | Security | ✅ Live, verified |
| `5a2be2d` | Cleanup | ✅ Live, verified |
| `3bb645b` | Polish | ✅ Live, verified |

Production URL: https://friendlyminter.vercel.app